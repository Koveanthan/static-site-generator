# python alias won't work in shell
python3 -m unittest discover -s src
# python3 src/testtextnode.py -- has same effect but -m unittest call all class
# beginning with test under src. No need to call all test* files individually.
